# ----------------------------------------------------------------------------- Grab info for hosts to setup

{{% if data.hostsToSetup|length > 0 %}}
- hosts: {{% for h in data.hostsToSetup%}}{{% if not loop.first %}}:{{% endif %}}{{{h}}}{{%endfor%}}
{{%endif%}}
